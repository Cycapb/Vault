@model IEnumerable<VaultDAL.Models.UserVault>
<div class="panel-body">
    <table class="table table-striped">
        <tr>
            <th>Name</th><th>Description</th><th>Open hours</th>
        </tr>
        @if (Model.Count() != 0)
        {
            foreach (var vault in Model)
            {
                <tr>
                    <td>@vault.Name</td>
                    <td>@vault.Description</td>
                    <td>
                        @vault.OpenTime:00 - @vault.CloseTime:00
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="3" class="text-center">No available vaults</td>
            </tr>
        }
    </table>
</div>
