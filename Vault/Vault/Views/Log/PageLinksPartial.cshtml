@model Vault.Models.PagingInfo
@for (var i = 1; i <= Model.TotalPages; i++)
{
    @Html.ActionLink(i.ToString(),"VaultLog",new {page = i}, new {@class=(i == Model.CurrentPage)?"btn btn-primary" : "btn btn-default"})
}
